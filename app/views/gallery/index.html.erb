<div class="g_contents">
  <div class="section-title">
    gallery
  </div>  
  <% @galleries.each do |post| %>
    <p class="g-item">
      <%= link_to image_tag(post.image.url), "/gallery/#{post.id}"%>
        <%# <%= image_tag post.image.url %> 
    </p>  
  <% end %>
</div>
<%= paginate(@galleries) %>